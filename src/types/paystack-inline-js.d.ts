// @paystack/inline-js ships no type definitions. The wrapper in
// src/lib/paystack.ts narrows the surface it actually uses, so an opaque
// module declaration is sufficient.
declare module "@paystack/inline-js";
